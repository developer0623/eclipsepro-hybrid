import { MetricLargeFilterPipe } from './metric-large-filter.pipe';

describe('MetricLargeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricLargeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
