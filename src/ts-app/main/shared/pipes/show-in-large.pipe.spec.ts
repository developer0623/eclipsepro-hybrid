import { ShowInLargePipe } from './show-in-large.pipe';

describe('ShowInLargePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowInLargePipe();
    expect(pipe).toBeTruthy();
  });
});
