import { ShowInMiniPipe } from './show-in-mini.pipe';

describe('ShowInMiniPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowInMiniPipe();
    expect(pipe).toBeTruthy();
  });
});
