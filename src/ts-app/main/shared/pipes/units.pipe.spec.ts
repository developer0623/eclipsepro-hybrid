import { UnitsFormatPipe } from './units.pipe';

describe('UnitsFormatPipe', () => {
  it('create an instance', () => {
    // const pipe = new UnitsFormatPipe();
    // expect(pipe).toBeTruthy();
  });
});
